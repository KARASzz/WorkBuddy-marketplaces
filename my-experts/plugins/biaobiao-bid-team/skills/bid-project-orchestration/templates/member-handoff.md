# 成员交接单

> 本交接单同时是子代理委派 prompt 的骨架：标标把成员定义 + skill 工作流 + 本交接单组装成自包含 prompt 后派发子代理。输出文件必须写绝对路径；子代理的成果必须落盘到该路径，只在对话里回复视为未完成。

- 项目 ID：
- 当前阶段：
- 招标版本：
- 已批准输入：
- 任务摘要：
- 输出文件（绝对路径）：
- 允许读取的目录（绝对路径）：
- 已核验证据：
- 待核验/冲突/缺失：
- 阻断项：
- 待用户确认：
- 建议下一步：
- 禁止扩大事项：
