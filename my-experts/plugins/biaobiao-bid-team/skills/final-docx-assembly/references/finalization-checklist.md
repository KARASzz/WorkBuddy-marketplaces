# 最终 Word 生成检查表

## 生成前

- **《招标包完整性核验表》已闭环：全部“阻塞级缺失”文件已到件并重新核验；未闭环不得生成 Word。**
- 门禁 #4 批准记录与输入文件完全对应。
- 招标版本、证据版本、价格版本一致。
- 阻断项为零，重大项已经处置或由用户明确接受。
- 不存在 `【待用户填写报价】`、其他待填占位符或内部批注。
- 输入顺序、封面、目录、商务、技术、价格和附件边界明确。
- 本地图片与附件存在，远程链接未作为必要内容。

## 生成中

- 只做格式转换，不改变事实、数字、单位、否定词和承诺。
- 官方表格不改变字段名、顺序和必填标识。
- 每个分册边界和明确分页意图得到保留。
- 文档另存为新文件，不覆盖 Markdown 或原件。

## 生成后

- Word 能正常打开，无修复提示。
- 封面、目录、章节和附件顺序完整。
- 表格没有截断、错列、丢行或数字变形。
- 图片清晰且未丢失，题注与引用对应。
- 项目名称、采购人、标段、日期、报价和大小写金额与最终 Markdown 一致。
- 页眉页脚、页码、签章位和装订提示适用。
- 人工签字、盖章、密封、加密、平台上传和递交回执仍在人工动作清单中。

生成 Word 不等于完成投标提交。
