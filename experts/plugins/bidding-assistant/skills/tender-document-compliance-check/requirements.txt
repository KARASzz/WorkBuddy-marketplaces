# 投标文件合规检查 — Python 依赖
# 安装：pip install -r requirements.txt

# Word 文档读写（投标文件提取 + 报告生成）
python-docx>=1.1.0

# PDF 文本提取
pdfplumber>=0.9.0

# 以下为可选依赖（若 pdfplumber 无法提取扫描件，由 scripts/ocr_extract.py 调用 RicheeAI OCR API）
# 无需额外安装，OCR 通过 HTTP API 调用，仅需设置 RICHEEAI_TOKEN 环境变量
